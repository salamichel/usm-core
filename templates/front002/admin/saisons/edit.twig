{% extends 'admin/layout.twig' %}
{% import 'admin/_macros.twig' as m %}
{% block title %}Modifier la saison{% endblock %}

{% block content %}

{# ── En-tête avec breadcrumb ── #}
<div class="relative overflow-hidden bg-slate-900 rounded-3xl p-6 md:p-8 text-white shadow-sm mb-8">
  <div class="relative">
    <nav class="flex items-center gap-1.5 text-xs text-slate-400 font-medium mb-2">
      <a href="{{ url('admin') }}" class="hover:text-white transition">Admin</a>
      <svg class="w-3 h-3 text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" /></svg>
      <a href="{{ url('admin/saisons') }}" class="hover:text-white transition">Saisons</a>
      <svg class="w-3 h-3 text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" /></svg>
      <span class="text-slate-200">Éditer</span>
    </nav>
    <h1 class="text-2xl font-black tracking-tight">Modifier la saison « {{ saison.libelle }} »</h1>
  </div>
</div>

{{ m.form_error(error ?? null) }}

<form method="POST" action="{{ url('admin/saisons/' ~ saison.id ~ '/edit') }}" class="space-y-6">
  <input type="hidden" name="_csrf_token" value="{{ csrf_token }}">

  <div class="bg-white border border-slate-100 rounded-3xl shadow-sm p-6 md:p-8 space-y-6 max-w-xl">
    {{ m.field_input('libelle', 'libelle', 'Libellé de la saison', saison.libelle ?? '', 'text', true, '2024-2025') }}
    {{ m.field_input('date_debut', 'date_debut', 'Date de début', saison.date_debut ?? '', 'date', true) }}
    {{ m.field_input('date_fin', 'date_fin', 'Date de fin', saison.date_fin ?? '', 'date', true) }}
  </div>

  <div class="flex items-center gap-3 pt-2">
    {{ m.btn_submit('Enregistrer') }}
    <a href="{{ url('admin/saisons') }}"
       class="inline-flex items-center gap-1.5 text-sm font-semibold text-slate-500 hover:text-slate-700 transition">
      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18" /></svg>
      Annuler
    </a>
  </div>
</form>
{% endblock %}
