{% extends 'base.twig' %}
{% block title %}{{ equipe.libelle }}{% if saison %} — {{ saison.libelle }}{% endif %} — USM Volley{% endblock %}

{% import '_gallery.twig' as components %}

{% block content %}
<div class="max-w-5xl mx-auto px-4 py-10">

  {# Breadcrumb #}
  <nav class="text-sm text-gray-500 mb-6">
    <a href="{{ url('/') }}" class="hover:underline">Accueil</a>
    <span class="mx-2">›</span>
    <a href="{{ url('equipes') }}" class="hover:underline">Équipes</a>
    <span class="mx-2">›</span>
    <span class="font-semibold text-gray-800">{{ equipe.libelle }}</span>
  </nav>

  <div class="flex items-baseline gap-4 mb-8">
    <h1 class="text-4xl font-black">{{ equipe.libelle }}</h1>
    <span class="border-2 border-black px-2 py-0.5 text-sm font-bold bg-amber-100">{{ equipe.categorie }}</span>
    {% if saison %}
      <span class="text-gray-400 text-sm font-semibold">{{ saison.libelle }}</span>
    {% endif %}
  </div>

  {# Galerie #}
  {{ components.gallery(photos) }}

  {# Liste des joueurs #}
  {% if joueurs|length %}
  <section class="mt-10">
    <h2 class="text-2xl font-black mb-6 border-b-4 border-black pb-2">Joueurs</h2>
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
      {% for j in joueurs %}
      <div class="border-4 border-black bg-white p-4 shadow-[4px_4px_0_#000]">
        <p class="font-black text-base leading-tight">{{ j.prenom }} {{ j.nom }}</p>
      </div>
      {% endfor %}
    </div>
  </section>
  {% endif %}

</div>
{% endblock %}

{% block head %}
{% if photos|length %}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/glightbox/dist/css/glightbox.min.css">
{% endif %}
{% endblock %}

{% block scripts %}
{% if photos|length %}
<script src="https://cdn.jsdelivr.net/npm/glightbox/dist/js/glightbox.min.js"></script>
<script>GLightbox({ selector: '.glightbox' });</script>
{% endif %}
{% endblock %}
