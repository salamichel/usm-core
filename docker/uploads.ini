; Aligne les limites PHP sur la taille acceptée par Photo::uploadSingle (10 Mo).
; On laisse une marge légère pour tenir compte de l'overhead du multipart/form-data.
upload_max_filesize = 12M
post_max_size = 12M
memory_limit = 128M
